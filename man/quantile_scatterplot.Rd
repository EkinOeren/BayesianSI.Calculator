% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantile_scatterplot.R
\name{quantile_scatterplot}
\alias{quantile_scatterplot}
\title{Generate Interactive Quantile Scatterplot with Ellipses}
\usage{
quantile_scatterplot(
  data = NULL,
  fixed_effects = NULL,
  random_params = NULL,
  by = "Single Chain Dataset",
  pred_dist = NULL,
  selected_by_values = NULL,
  tolerance_level = 95,
  ellipse_confidence = 95,
  multiplication_factor = 1,
  log_normal = FALSE,
  covariate_cols = NULL,
  covariate_values = NULL
)
}
\arguments{
\item{data}{A data frame containing MCMC samples. Required if pred_dist is NULL.}

\item{fixed_effects}{Character vector of column names for fixed effects. Required if pred_dist is NULL.}

\item{random_params}{Character vector of column names for random parameters. Required if pred_dist is NULL.}

\item{by}{Character string specifying the grouping variable. Default is "Single Chain Dataset".}

\item{pred_dist}{Optional pre-calculated predicted distributions from calculate_predicted_distributions().
If provided, data, fixed_effects, and random_params are ignored.}

\item{selected_by_values}{Optional vector of specific by values to include in the plot.
If NULL, all by values are shown. Default is NULL.}

\item{tolerance_level}{Numeric value between 0 and 100 for tolerance interval percentage. Default is 95.}

\item{ellipse_confidence}{Numeric value between 0 and 100 for credible ellipses. Default is 95.}

\item{multiplication_factor}{Positive numeric value to adjust variance components. Default is 1.}

\item{log_normal}{Logical indicating whether to transform predictions to log-normal scale. Default is FALSE.}

\item{covariate_cols}{Character vector of column names for covariates. Default is NULL.}

\item{covariate_values}{Numeric vector of values to multiply with covariates. Default is NULL.}
}
\value{
A plotly object containing the interactive quantile scatterplot with ellipses.
}
\description{
This function creates an interactive scatterplot visualizing the relationship between lower and upper
quantiles of posterior predictive distributions from MCMC samples, with confidence ellipses.
It uses plotly for interactivity and can work with either raw data or pre-calculated distributions.
}
\details{
The function creates a scatterplot where:
\itemize{
\item Each point represents a single MCMC sample
\item X-axis shows the lower quantile
\item Y-axis shows the upper quantile
\item Points are grouped by the specified 'by' variable
\item Confidence ellipses show the concentration of points
\item Diamond markers show the mean position for each group
\item Interactive tooltips provide detailed information
}
}
\examples{
\dontrun{
# Example 1: From raw data without covariates
plot1 <- quantile_scatterplot(
  data = mcmc_samples,
  fixed_effects = "beta0",
  random_params = "sigma2",
  by = "Group",
  tolerance_level = 95,
  ellipse_confidence = 90
)

# Example 2: From raw data with covariates
plot2 <- quantile_scatterplot(
  data = mcmc_samples,
  fixed_effects = "beta0",
  random_params = "sigma2",
  by = "Group",
  covariate_cols = "age",
  covariate_values = 65,
  tolerance_level = 90
)

# Example 3: From pre-calculated distributions
pred_dist <- calculate_predicted_distributions(...)
plot3 <- quantile_scatterplot(
  pred_dist = pred_dist,
  tolerance_level = 95,
  ellipse_confidence = 95
)

# Example 4: Filter specific groups
plot4 <- quantile_scatterplot(
  data = mcmc_samples,
  fixed_effects = "beta0",
  random_params = "sigma2",
  by = "Group",
  selected_by_values = c("A", "B"),
  tolerance_level = 90
)
}

}
