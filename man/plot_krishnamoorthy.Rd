% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_krishnamoorthy.R
\name{plot_krishnamoorthy}
\alias{plot_krishnamoorthy}
\title{Create Krishnamoorthy Two-Sided Tolerance Interval Plot}
\usage{
plot_krishnamoorthy(
  data,
  by_value,
  optimal_point,
  tolerance_level,
  confidence_level,
  log_normal = FALSE
)
}
\arguments{
\item{data}{A data frame containing lower and upper quantiles}

\item{by_value}{Character or numeric, the specific by-value to plot}

\item{optimal_point}{List containing Krishnamoorthy method results with elements:
\itemize{
\item g1: optimal lower bound
\item g2: optimal upper bound
\item gamma_achieved: achieved confidence level
}}

\item{tolerance_level}{Numeric, tolerance level percentage (between 0 and 100)}

\item{confidence_level}{Numeric, confidence level percentage (between 0 and 100)}

\item{log_normal}{Logical, whether to transform results from log space (default = FALSE)}
}
\value{
A plotly object containing the Krishnamoorthy plot
}
\description{
Creates an interactive plot showing the Krishnamoorthy method results for two-sided
tolerance intervals, including scatter points, optimal line, and optimal point.
}
\examples{
\dontrun{
# First calculate predicted distributions and optimal points
pred_dist <- calculate_predicted_distributions(...)
gP_mean <- calculate_gP_mean(pred_dist, 90)
optimal_results <- find_optimal_point(pred_dist, gP_mean, 80)

# Create plot for a specific by value
plot <- plot_krishnamoorthy(
  data = pred_dist,
  by_value = "60",
  optimal_point = optimal_results[["60"]],
  tolerance_level = 90,
  confidence_level = 80
)
}

}
